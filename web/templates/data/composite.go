package main
/*
import (
	"text/template"
	"os"
	"log"
)

var tpl *template.Template

func init() {
	tpl = template.Must(template.ParseFiles("tpl3.gohtml"))
}

func main() {

	names := []string{"Ethan", "Michael", "Corey", "Tristen", "Aaron"}
	err := tpl.Execute(os.Stdout, names)
	if err != nil {
		log.Fatalln(err)
	}
}

*/